# Handoff — June 17, 2026

## Site
- **Production URL:** https://foundationalaisystem.com
- **Repo:** https://github.com/AndrewMarasigan31/foundational-ai-website
- **Deploy:** Manual via `cd /Users/andrew/foundational-ai-website && vercel --prod` (GitHub auto-deploy not connected yet)
- **Stack:** Next.js 14 App Router, Tailwind, Framer Motion, GHL for CRM/booking

---

## What Was Completed This Session

### SEO & Meta
- `lib/siteConfig.ts` — single source of truth for `SITE_URL`, `SITE_NAME`, `OG_IMAGE`
- `metadataBase` + global OG/Twitter defaults in `app/layout.tsx`
- Per-page metadata on all 6 pages: Home, About, Services, Contact, Privacy, Terms
- "use client" pages (About, Services, Contact) refactored — content extracted to `*Content.tsx` siblings so `page.tsx` can export `metadata` as a server component
- Title template: `%s | Foundational AI Systems` (pipe separator, no em-dash)
- Positioning: **"AI-Powered Local Growth"** across all titles and descriptions

### OG Image
- `app/opengraph-image.tsx` — dynamic Next.js OG image, generated at runtime
- Uses Bricolage Grotesque 800 TTF (stored at `public/fonts/BricolageGrotesque-800.ttf`)
- Logo loaded from `public/logo.png` via `webpackIgnore` fs import
- Content: "AI-Powered / Local Growth (gold) / for Small Businesses." + service tags + domain
- Live at: https://foundationalaisystem.com/opengraph-image

### Schema
- LocalBusiness JSON-LD (`ProfessionalService` type) in `<head>` on every page via `layout.tsx`
- Fields: name, description, url, image, priceRange, areaServed, knowsAbout
- Missing (add when available): telephone, address, sameAs (social links)

### Mobile QA
- Checked all pages at 390×844 (iPhone 14 Pro viewport)
- No layout breaks, no overflow, nav hamburger works, form is tappable
- Empty sections in headless screenshots are scroll-triggered animations — verified fine on real device

---

## Pending — Needs CEO Input
1. **Founder bio** — 2–3 sentences for the About page (no person on it currently)
2. **Headshot photo** — any clean well-lit photo, will handle cropping
3. **Business phone number** — optional, for schema markup
4. **Social profile links** — LinkedIn, Instagram etc. for schema `sameAs`

## Pending — Needs Action
1. **Form submission test** — submit a real entry at `/contact` and confirm it lands in GHL
2. **Connect GitHub to Vercel** — go to Vercel → Settings → Git → Connect Repository → `AndrewMarasigan31/foundational-ai-website`

---

## Next Session — Car Rental Sample Project

### Goal
Build a `/work/car-rental` page using the existing spec doc as content, then integrate it into outreach.

### Source file
`foundational-ai-website/FASLLC - Car Rental Growth System.html`

### Structure of the spec doc
- How It Works (5 steps)
- What's Included
- Timeline
- Pricing
- Optional Add-Ons
- What Changes When This Is Live
- Simple Promise

### Plan
1. Build `/app/work/car-rental/page.tsx` — clean Next.js page styled to match site branding
2. Add metadata + OG tags for the page
3. Label honestly as "Sample Project — Car Rental Growth System"
4. CTA: "Want this for your business? Book a call."
5. Use the URL in cold outreach to car rental businesses as a leave-behind

### Outreach integration
- Reference in cold email: "I mapped out exactly what this looks like for car rental businesses — [link]"
- No PDF attachment needed — link is cleaner and trackable
- Works as proof of methodology for any industry, not just car rental

---

## Key Files
| File | Purpose |
|---|---|
| `lib/siteConfig.ts` | Site URL, name, OG image constants |
| `app/layout.tsx` | Global metadata, JSON-LD schema, GA4 |
| `app/opengraph-image.tsx` | Dynamic OG image |
| `public/fonts/BricolageGrotesque-800.ttf` | Font for OG image |
| `app/*/page.tsx` | Per-page metadata (server components) |
| `app/*/[Page]Content.tsx` | Client components extracted from pages |
| `docs/superpowers/plans/2026-06-17-og-tags-and-schema.md` | Original implementation plan |
