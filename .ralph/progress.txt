## 2026-06-13 - US-023
- Built first three service sections in app/services/page.tsx
- Exported GBPStarAnimation, BlogTypingAnimation, AISearchAnimation from components/ServicesSection.tsx
- Replaced placeholder sections with full ServiceSectionBlock components for GBP Audit, Local SEO Content, AI Search Visibility
- Alternating layout: GBP (copy left / visual right, bg-[#021524]), Content (visual left / copy right, bg-[#000f1d]), AI Search (copy left / visual right, bg-[#021524])
- Each section: icon, heading, 2-3 sentence description, 3-4 bullet deliverables, gold BookCTA button
- Visual panels wrap the reused animation components in a branded rounded card with supplementary detail
- Remaining 3 service placeholder sections (lead-reactivation, website, performance-tracking) preserved with correct ids for sticky nav
- Typecheck passes, build passes (3.09kB /services page); manual browser verification needed (no dev-browser MCP)
- **Learnings for future iterations:**
  - Export animation sub-components from their parent file (not a separate file) so they stay co-located but are importable by other pages
  - ServiceSectionBlock reverse prop pattern: `md:[&>*:first-child]:order-2` flips the two-column order on desktop without touching mobile
  - Shared BookCTA extracted as a small helper component avoids prop-drilling link/styles across every section
---

## 2026-06-13 - US-020
- Added 4 inline animation sub-components to components/ServicesSection.tsx
- GBPStarAnimation: useMotionValue + Framer Motion animate(), 3.2 → 4.9 star rating over 1.5s easeOut, triggered by useInView once:true
- BlogTypingAnimation: useEffect interval types out blog post title character by character (40ms/char), starts on scroll entry
- LighthouseAnimation: requestAnimationFrame loop counts score 54 → 98 over 1.5s cubic ease-out with color transition red → orange → green
- RankTrackingAnimation: setInterval decrements #12 → #3 at 180ms each, gold highlight + "Top 3 ✓" label on completion
- All sub-components use useRef(false) started guard to prevent re-triggering; all hold final state once complete
- Added "use client" to ServicesSection.tsx (required for hooks in sub-components)
- Removed old MockGBPPanel, replaced with GBPStarAnimation in GBP card mockPanel slot
- Added mockPanel to Content, Website, Tracking cards (previously null)
- Typecheck passes, build passes (55.9kB page); manual browser verification needed (no dev-browser MCP)
- **Learnings for future iterations:**
  - JSX elements in data arrays (e.g. `mockPanel: <GBPStarAnimation />`) work fine — React instantiates hooks when the element renders, not when the array is defined
  - `useRef(false)` started guard prevents double-trigger on React strict mode remount in dev
  - `performance.now()` is safe in component code (not restricted like in workflow scripts)
  - `requestAnimationFrame` loop is cleaner than `setInterval` for count animations that track wall-clock time precisely
  - Framer Motion `useMotionValue` + `animate()` (function import) is the correct pattern for animated numbers — `motion.span` animate prop is for layout-tied animations
---

## 2026-06-13 - US-019
- Animated SVG process line in HowItWorksSection replacing the static `bg-white/10` div
- Added "use client" to HowItWorksSection (required for useRef/useInView/useAnimation hooks)
- Created AnimatedProcessLine sub-component: SVG with motion.line using pathLength 0→1, useInView + useAnimation, 0.2s delay, 1s ease-in-out
- Each of the 4 step columns now wrapped in its own AnimatedSection with stagger delays: 0s, 0.15s, 0.3s, 0.45s
- SVG line is hidden on mobile (hidden md:block), desktop only
- Step 1 circle retains gold fill + glow; steps 2-4 transition to gold on hover (group/group-hover pattern preserved)
- Typecheck passes, build passes; manual browser verification needed (no dev-browser MCP)
- **Learnings for future iterations:**
  - Framer Motion `pathLength` on `motion.line` handles strokeDasharray/strokeDashoffset automatically — cleaner than manual dash calculations
  - `useAnimation()` + `useInView()` + `useEffect` pattern: store controls, trigger `controls.start()` when `isInView` flips true
  - SVG with `height="1"` and `overflow: visible` style is the right way to create a hairline SVG connector that doesn't clip
  - When wrapping grid children in AnimatedSection (motion.div), group/group-hover still works since group is on the inner div
  - Section components can add "use client" when hooks are needed inline — the pattern of keeping sections server-only is a preference, not a requirement
---

## 2026-06-13 - US-018
- Refactored components/HeroSection.tsx from md:grid-cols-2 split layout to single centered column
- Added eyebrow pill: 'Local SEO for US Small Businesses' with bolt icon (was missing from prior implementation)
- Layout order: eyebrow pill → H1 → subhead (max-w-3xl) → CTAs → TrustBar → LocalPackVisual
- LocalPackVisual wrapped in AnimatedSection (direction='up', delay=0.3) with max-w-xl centering constraint
- Removed "use client" directive (not needed; client children handle their own boundaries)
- Grid background pattern retained; mobile graceful single column at any breakpoint
- Typecheck and build both pass; manual browser verification needed (no dev-browser MCP)
- **Learnings for future iterations:**
  - Centered hero pattern: `flex flex-col items-center text-center` on the container, copy at `max-w-3xl`, visual at `max-w-xl`
  - AnimatedSection on a below-fold visual within the hero creates a scroll-triggered reveal while keeping the above-fold content static
  - Eyebrow pill pattern: `inline-flex items-center gap-2 bg-[#0e2131] border border-[#C9A227]/30 text-[#C9A227] text-sm font-medium px-4 py-1.5 rounded-full`
---

## 2026-06-13 - US-016
- Replaced DashboardVisual.tsx (Recharts bar chart + stat cards) with GBPOptimizationSequence component
- Updated DashboardSection.tsx: pill label → "The Work", heading → "What Optimization Looks Like", new subhead
- 4 steps animate in with Framer Motion stagger (0.2s between each), triggered by useInView
- Step 1 (SEO Content Added): content block with blog post tag fades/slides in
- Step 2 (Category Cleanup): wrong categories get line-through + fade to 35% opacity; correct ones pulse in with CSS transition delays
- Step 3 (Business Description Refresh): skeleton placeholders fade out, real copy fades in (absolute positioned overlay swap)
- Step 4 (Photo & Content Recommendations): 4 image slots fill with gold check badges and label text
- Each step header shows gold checkmark icon via Framer Motion scale-in animation
- Removed recharts from package.json (only dependency was DashboardVisual.tsx)
- Build and typecheck both pass
- Manual browser verification needed (no dev-browser MCP)
- **Learnings for future iterations:**
  - Overlay swap pattern for before/after content: both layers `absolute inset-4`, one fades out while other fades in via CSS transition with delay
  - Framer Motion `custom` prop on variants enables per-item delay without creating N variants
  - CSS transition + `isInView` boolean is sufficient for simple opacity/transform effects; no need for Framer Motion for every child element
  - Removing a package from package.json does not uninstall it from node_modules (run `npm install` if needed), but build succeeds if nothing imports it
---

## 2026-06-13 - US-015
- Enhanced components/LocalPackVisual.tsx with a pure SVG city map background
- SVG map (300x260 viewBox): dark navy base, road grid (main highways + side streets), city block rectangles, a park area (green tinted)
- 3 gold pins positioned in upper/mid map area with double pulse rings via SVG `<animate>` (no JS) — staggered begin delays (0s, 0.35s+0.95s, 0.7s+1.3s)
- 5 gray muted competitor pins scattered across the map
- Gradient vignette linearGradient (transparent → 97% opacity dark) covers bottom 54% for card readability
- Listing cards repositioned to overlay bottom of map with `backdrop-blur-md` and 80% opacity background
- Top 3 Local Pack badge floats above cards in the overlay layer
- Component is fully responsive (SVG + absolute overlay pattern)
- No external image dependencies — pure SVG + Framer Motion for card stagger
- Build and typecheck both pass; manual browser verification needed (no dev-browser MCP)
- **Learnings for future iterations:**
  - SVG `<animate>` pulse rings: set initial `strokeOpacity="0"` on the circle, then animate `stroke-opacity` from value to 0 — avoids flash on mount
  - SVG `<defs>` gradients with `id` work fine inside React SVG; put `<defs>` before all geometry for clarity
  - Pattern for map overlay UI: `relative overflow-hidden rounded-2xl` container + `<svg viewBox>` sets aspect ratio + `absolute inset-0 flex flex-col justify-end` for overlay
  - `backdrop-blur-md` on cards requires `bg-[color]/opacity` (transparent background) to show the blur effect
---

## 2026-06-13 - US-014
- Verified homepage composition: all sections wired in correct order in app/page.tsx
- app/layout.tsx already renders Nav and Footer around {children} (wired in US-002/US-003)
- app/page.tsx already composes: HeroSection, DashboardSection, ServicesSection, HowItWorksSection, WhyUsSection, FAQSection, CTABand (completed by US-013)
- Smooth scroll: `scroll-behavior: smooth` already present in globals.css html selector
- No code changes required — final integration was achieved incrementally across prior stories
- npm run build and typecheck both pass; static export at / (47.5kB)
- Manual browser verification needed (no dev-browser MCP available in this environment)
- **Learnings for future iterations:**
  - US-014 composition stories are "free" if each section story updates app/page.tsx incrementally
  - scroll-behavior: smooth on html element is all that's needed for anchor-link scrolling in Next.js App Router
---

## 2026-06-13 - US-013
- Built components/CTABand.tsx: closing CTA section before footer
- Heading "Start with a free audit call. No pitch, no pressure." with gold span on second clause
- Subhead from current site copy (no obligation framing)
- Two buttons: gold filled (Book a Free Audit Call → /contact) and outline (See the Services → /services)
- Subtle gold radial-gradient glow via inline style background
- AnimatedSection wraps all content
- Mobile: buttons are full-width (w-full sm:w-auto), stacked via flex-col sm:flex-row
- Updated app/page.tsx to render CTABand after FAQSection
- Typecheck and build pass
- **Learnings for future iterations:**
  - CTABand does NOT need "use client" — AnimatedSection is a client component imported as a child
  - Radial glow pattern: `radial-gradient(ellipse 70% 60% at 50% 50%, rgba(201,162,39,0.10) 0%, transparent 70%)` applied via inline style
  - Full-width mobile buttons: `w-full sm:w-auto` on each button + `flex-col sm:flex-row` on the container
---

## 2026-06-13 - US-012
- Built components/FAQSection.tsx: 6-item accordion FAQ section
- "use client" component; each FAQItem has independent useState(false) for open/close
- AnimatePresence + motion.div animates height 0→auto and opacity on expand/collapse
- 2-column grid (desktop), single column (mobile)
- AnimatedSection wraps header and grid (0.1s delay on grid)
- Set up Jest testing infrastructure: jest.config.js, jest.setup.ts, @types/jest, installed jest/jsdom/@testing-library
- Added "types": ["jest", "@testing-library/jest-dom"] to tsconfig.json for typecheck
- __tests__/FAQSection.test.tsx: 6 tests covering render, open/close, aria-expanded, multi-item independence
- Updated app/page.tsx to render FAQSection below WhyUsSection
- Tests pass (6/6), typecheck passes, build succeeds
- **Learnings for future iterations:**
  - Jest setup: `setupFilesAfterEnv` (not `setupFilesAfterFramework`) is the correct config key
  - Add `"types": ["jest", "@testing-library/jest-dom"]` to tsconfig to avoid typecheck errors for jest globals
  - Mock framer-motion AnimatePresence in tests: render children directly so animation state doesn't block assertions
  - Mock AnimatedSection in tests to unwrap children for simpler query targeting
---

## 2026-06-13 - US-011
- Built components/WhyUsSection.tsx: three differentiator cards section
- 3-column grid (desktop), single column (mobile)
- Each card: surface bg (#0e2131), white/10 border, faded gold number at 35% opacity (absolute positioned, top-right)
- AnimatedSection wraps header (no delay) and cards grid (0.1s delay)
- Updated app/page.tsx to render WhyUsSection below HowItWorksSection
- Build and typecheck pass
- **Learnings for future iterations:**
  - Faded number uses `text-[#C9A227]/[0.35]` with `absolute` positioning + `select-none pointer-events-none`
  - Card content needs `relative z-10` to sit above the absolute-positioned faded number
  - `overflow-hidden` on the card clips the large number neatly at card edges
---

## 2026-06-13 - US-010
- Built components/HowItWorksSection.tsx: four-step process section
- 4 steps in a 4-column grid (desktop), single column stack (mobile)
- Horizontal connecting line (hidden md:block) positioned behind circles using absolute positioning at top-8
- Step 1 circle: gold bg, gold border, gold glow shadow; Steps 2–4: muted, hover → gold border + text
- AnimatedSection wraps both header and steps grid (0.1s delay on steps)
- Updated app/page.tsx to render HowItWorksSection below ServicesSection
- Build and typecheck pass
- **Learnings for future iterations:**
  - Connecting line trick: `absolute top-8 left-[12.5%] right-[12.5%] h-px bg-white/10 z-0` with grid items at `z-10` keeps line behind circles
  - `group` class on the column container + `group-hover:` on inner elements enables hover effects on children without JS
  - `&apos;` needed for apostrophes in JSX text to avoid linting errors
---

## 2026-06-13 - US-009
- Built components/ServicesSection.tsx: asymmetric 12-column bento grid of 4 service cards
- GBP card (col-span-8): manage_search icon, description, 2 checklist items, inline mock GBP panel sub-component
- Local SEO Content (col-span-4): article icon, short description
- Website Built to Convert (col-span-4): web icon, short description
- Performance Tracking (col-span-8): monitoring icon, description
- All cards: surface bg (#0e2131), white/10 border, gold border + -translate-y-1 on hover
- AnimatedSection wraps both the header and the grid (0.1s delay on grid)
- Updated app/page.tsx to render ServicesSection below DashboardSection
- Build and typecheck pass
- **Learnings for future iterations:**
  - 12-column bento layout: `grid-cols-1 md:grid-cols-12` with per-card `md:col-span-N`
  - Hover animation via Tailwind: `hover:border-[#C9A227]/60 hover:-translate-y-1` (no Framer Motion needed)
  - Inline sub-components (MockGBPPanel) keep the file self-contained for complex card content
  - `shrink-0` on inline icons prevents them from being squished by long adjacent text
---

## Codebase Patterns
- Next.js 14 App Router project at repo root; app/ directory, tailwind.config.ts, postcss.config.js, next.config.js
- Tailwind custom colors: primary #C9A227, background #021524, surface #0e2131, on-surface #d1e5fb, outline #99907b
- TypeScript target must be ES2017+ (not ES5 — deprecated in TS 6+)
- CSS imports in layout.tsx require a css.d.ts module declaration file
- framer-motion and recharts are installed; recharts requires dynamic import with ssr: false
- Plus Jakarta Sans via next/font/google; Material Symbols via <link> in layout head
- Jest testing: setupFilesAfterEnv (not setupFilesAfterFramework); add "types": ["jest", "@testing-library/jest-dom"] to tsconfig; mock framer-motion and AnimatedSection in component tests

---

## 2026-06-13 - US-001
- Scaffolded Next.js 14 App Router project at repo root
- Installed: next@14, react@18, react-dom@18, framer-motion, recharts, tailwindcss@3, postcss, autoprefixer, typescript, @types/*
- Files created: package.json, tsconfig.json, tailwind.config.ts, postcss.config.js, next.config.js, app/layout.tsx, app/page.tsx, app/globals.css, app/css.d.ts, public/logo.png, next-env.d.ts, .gitignore
- npm run build and typecheck both pass
- **Learnings for future iterations:**
  - TypeScript 6 deprecates target=ES5; use ES2017
  - CSS side-effect imports need app/css.d.ts with `declare module "*.css"`
  - Plus Jakarta Sans font variable is --font-plus-jakarta-sans
  - logo.png is at public/logo.png (copied from assets/logo.png)
---

## 2026-06-13 - US-002
- Built components/Nav.tsx: sticky glassmorphism nav with logo, links, Book a Call CTA, and mobile hamburger drawer
- Updated app/layout.tsx to import and render <Nav /> above {children}
- Files changed: components/Nav.tsx (new), app/layout.tsx
- npm run build and typecheck both pass
- **Learnings for future iterations:**
  - Use `"use client"` on components that use useState or usePathname
  - usePathname from next/navigation for active link detection
  - Components live in /components directory (already in Tailwind content scan)
  - Material Symbols class is `material-symbols-outlined` (not `material-icons`)
  - Nav was pre-wired into layout.tsx here; US-014 just confirms it's in place
---

## 2026-06-13 - US-003
- Built components/Footer.tsx: 4-column responsive footer with brand, services, company, and legal columns
- Includes LinkedIn and Twitter SVG icons in brand column; copyright line at bottom
- Updated app/layout.tsx to import and render <Footer /> below {children}
- Files changed: components/Footer.tsx (new), app/layout.tsx
- Typecheck passes
- **Learnings for future iterations:**
  - Footer uses inline SVG for social icons (no external icon library needed)
  - Grid collapses via `grid-cols-1 sm:grid-cols-2 md:grid-cols-4`
  - Footer does NOT need "use client" — it has no interactivity
---

## 2026-06-13 - US-005
- Built components/LocalPackVisual.tsx: animated Google local pack mockup
- Three mock business listing cards with name, star rating (★), and review count
- Map pin SVG icon at top with gold glow drop-shadow
- "Top 3 Local Pack" badge with animate-pulse
- Framer Motion staggerChildren (0.15s each) on container variants, cards fade+slide up on mount
- Rank number circles with gold accent, hover border highlight on cards
- Subtle gold radial glow background (z-10, blur-2xl)
- Files changed: components/LocalPackVisual.tsx (new)
- Typecheck passes
- **Learnings for future iterations:**
  - Framer Motion `ease` in variant `transition` must be typed `as const` to match `Easing` type
  - Use `staggerChildren` in containerVariants + per-item `cardVariants` for stagger effect
  - `animate-pulse` is a Tailwind utility, works independently of Framer Motion
---

## 2026-06-13 - US-006
- Built components/DashboardVisual.tsx: animated stats dashboard with Recharts bar chart and count-up stat cards
- Bar chart dynamically imported with ssr: false using `dynamic(() => import("recharts").then(...))` pattern returning an inner function component
- 6 months of mock traffic data (Jan–Jun), bar colors gradient from muted gold to bright gold for latest bar
- Two stat cards: 42.8K (Total Views) and 94% (Retention Rate) with eased count-up via useEffect + setInterval
- Count-up triggered by useInView (once: true), easing: cubic ease-out
- Browser chrome: 3 macOS traffic-light dots (red/yellow/green), URL bar with site address
- Files changed: components/DashboardVisual.tsx (new)
- Typecheck and build both pass
- **Learnings for future iterations:**
  - Recharts `formatter` prop on Tooltip receives `value: ValueType | undefined` — must handle undefined, not type as `number` directly
  - Pattern for SSR-safe recharts: `dynamic(async () => { const { BarChart, ... } = await import("recharts"); return function Inner() {...}; }, { ssr: false })`
  - `Cell` component from recharts used for per-bar coloring inside `Bar` — must be a child of `Bar`, not `BarChart`
  - Count-up easing via `1 - Math.pow(1 - progress, 3)` gives smooth cubic ease-out without external libs
---

## 2026-06-13 - US-007
- Built components/HeroSection.tsx: split-layout hero with eyebrow pill, H1, subhead, two CTA buttons, and LocalPackVisual on the right
- Updated app/page.tsx to render HeroSection
- Grid background uses inline backgroundImage style for the subtle dot grid pattern
- CTA buttons: gold filled (Book a Free Audit Call → /contact) and outline (See the Services → /services)
- Mobile: single column stacked layout; desktop: 2-column grid
- Files changed: components/HeroSection.tsx (new), app/page.tsx
- Typecheck and build both pass
- **Learnings for future iterations:**
  - HeroSection does not need "use client" — LocalPackVisual (client component) is imported as a child; Next.js handles the boundary
  - Eyebrow pill uses material-symbols-outlined class for the bolt icon, consistent with Nav pattern
  - Gold glow on primary CTA via `shadow-[0_0_20px_rgba(201,162,39,0.3)]` — reusable pattern for CTA buttons
---

## 2026-06-13 - US-004
- Built components/AnimatedSection.tsx: scroll-triggered animation wrapper using Framer Motion
- Uses useInView with `once: true` and `margin: "-10% 0px"` for natural trigger timing
- Supports direction prop ('up' | 'left' | 'right', default 'up') and delay prop (number, seconds)
- Entry animation: opacity 0→1 + translate 24px→0, 0.5s easeOut
- Files changed: components/AnimatedSection.tsx (new)
- Typecheck passes
- **Learnings for future iterations:**
  - AnimatedSection requires "use client" (uses useRef and useInView hooks)
  - useInView margin: "-10% 0px" prevents animations triggering only when element fully enters viewport
  - Variants object pattern cleanly maps direction prop to Framer Motion initial/animate states
  - All section components (US-007 through US-013) should import AnimatedSection from components/AnimatedSection
---

## 2026-06-13 - US-008
- Built components/DashboardSection.tsx: Business Growth Engine section with darker background (#000f1d)
- Section pill label "Business Growth Engine", centered heading, subhead, and DashboardVisual below
- Two AnimatedSection wrappers: one for the header copy (no delay) and one for DashboardVisual (0.15s delay)
- Updated app/page.tsx to import and render DashboardSection below HeroSection
- Files changed: components/DashboardSection.tsx (new), app/page.tsx
- Build and typecheck pass
- **Learnings for future iterations:**
  - DashboardSection does NOT need "use client" — DashboardVisual is a client component imported as a child
  - Two separate AnimatedSection wrappers with staggered delay give heading + visual independent entrance animations
  - Background #000f1d for this section creates visible contrast against adjacent #021524 sections
---

## 2026-06-13 - US-017
- Built components/TrustBar.tsx: stateless presentational component with 4 category pills
- Pills: 'Restaurants', 'Law Firms', 'Home Services', 'Dental' — surface bg (#0e2131), white/10 border, muted text (#99907b)
- Small muted label above: 'Trusted by local businesses including' at 70% opacity
- flex-wrap ensures natural wrapping on mobile
- No Framer Motion — enters as part of hero flow
- Integrated into components/HeroSection.tsx below CTA buttons
- Typecheck passes; manual browser verification needed (no dev-browser MCP)
- **Learnings for future iterations:**
  - TrustBar is purely presentational — no "use client" needed
  - Color pattern for non-competing UI elements: use #99907b (outline/muted) instead of gold
  - flex-wrap gap-2 is sufficient for pill wrapping across breakpoints
---

## 2026-06-13 - US-021
- Created components/CustomerJourneySection.tsx — 6-node horizontal customer journey funnel
- 6 nodes: Search → Found → Visits Site → Calls/Books → Goes Cold → Books Again
- AnimatedJourneyLine: 3-segment horizontal line using motion.div with x: "-100%" → "0%" inside overflow-hidden wrappers (slide-reveal pattern)
  - Segment 1 solid white/10 (nodes 1→4): 0.7s, delay 0.2s
  - Segment 2 dashed red repeating-gradient (nodes 4→5): 0.25s, delay 0.9s
  - Segment 3 gold (nodes 5→6): 0.25s, delay 1.15s
  - Total line draw time: ~1.4s from scroll entry
- Service impact tags stagger in with AnimatedSection delays 1.4s–1.8s (after line finishes)
- "Books Again" node: gold fill + glow shadow + task_alt checkmark icon
- "Goes Cold" node: red/warning border + schedule icon
- Desktop: grid-cols-6, AnimatedJourneyLine positioned at top: 28px (center of w-14 circles)
- Mobile: flex-col, each node is [circle + vertical connector | label + tags], connector uses repeating-gradient for dashed segment
- Added to app/page.tsx between ServicesSection and HowItWorksSection
- Build passes (56.9 kB page); typecheck passes; manual browser verification needed (no dev-browser MCP)
- **Learnings for future iterations:**
  - slide-reveal pattern for line animation: `overflow-hidden` parent + `motion.div` with `x: "-100%" → "0%"` is cleaner than pathLength on SVG and works with CSS-based dashes (repeating-gradient)
  - repeating-linear-gradient(90deg, ...) creates dashed horizontal lines without SVG; repeating-linear-gradient(to bottom, ...) for vertical dashes on mobile
  - For 6-column grids, node centers are at 8.33%, 25%, 41.67%, 58.33%, 75%, 91.67%
  - `nodeTagDelays` IIFE pattern (immediately invoked function expression) at module level is clean for precomputing stagger delays
  - `style={{ minHeight: "2rem", ...mobileConnectorStyle(i) }}` — spreading style objects in JSX works for conditional mobile connector styles
---

## 2026-06-13 - US-022
- Created app/services/page.tsx as a client component (required for IntersectionObserver hooks)
- Hero section: centered layout with H1 'The Work We Do', subhead, gold 'Book a Free Audit Call' CTA, grid background pattern matching HeroSection
- Sticky StickyServiceNav sub-component: 6 anchor links (GBP Audit, Local SEO Content, AI Search, Lead Reactivation, Website, Performance Tracking), backdrop-blur, gold underline on active via IntersectionObserver
- IntersectionObserver uses rootMargin "-40% 0px -55% 0px" to activate link when section is near center of viewport
- Placeholder sections for each service anchor — content filled in US-023 and US-024
- Typecheck passes, build passes (1.53kB /services page); manual browser verification needed (no dev-browser MCP)
- **Learnings for future iterations:**
  - Services page is "use client" because StickyServiceNav needs IntersectionObserver — the page itself becomes a client component
  - Sticky nav `top-[64px]` offsets below the Nav bar (assumed ~64px height); adjust if Nav height changes
  - IntersectionObserver rootMargin "-40% 0px -55% 0px" gives a centered active trigger zone — activates the section whose content is crossing the middle of the viewport
  - Placeholder sections with the correct `id` attributes must be present in US-022 so the sticky nav links work before US-023/024 add real content
---
