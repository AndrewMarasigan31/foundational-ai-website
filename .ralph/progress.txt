## 2026-06-13 - US-039
- Refactored HeroSection from single centered column to two-column grid (grid-cols-1 md:grid-cols-2)
- Left column: h1, subtext paragraph, CTA buttons, TrustBar
- Right column: LocalPackCard (no AnimatedSection wrapper)
- Removed AnimatedSection import and wrapper from HeroSection.tsx entirely
- Removed min-h-[90vh] and flex items-center (replaced by grid with items-center on the grid container)
- Adjusted padding from py-24 md:py-32 to py-20 md:py-28 to account for shorter vertical layout
- Files changed: components/HeroSection.tsx
- HTML verified: grid grid-cols-1 md:grid-cols-2 rendered in SSR output; no AnimatedSection in output
- Browser testing unavailable (Chrome not installed); manual verification recommended
- **Learnings for future iterations:**
  - Two-column grid layout replaces flexbox centering: use `grid grid-cols-1 md:grid-cols-2 gap-X items-center` on the container
  - When dropping min-h-[90vh] + flex items-center, the vertical rhythm comes from padding on the grid container instead
  - LocalPackCard works directly in a grid cell — no wrapper div for max-w-xl needed, can use flex justify-center on the right column
---

## 2026-06-13 - US-038
- Removed AnimatedSection import and all three AnimatedSection wrappers from app/contact/page.tsx (hero section and both two-column panel wrappers)
- Added order-last md:order-first to the trust signals (left) column div
- Added order-first md:order-last to the form (right) column div so form appears above trust signals on mobile
- Files changed: app/contact/page.tsx
- Typecheck passes; browser testing unavailable (Chrome not installed)
- **Learnings for future iterations:**
  - CSS order swap pattern for mobile-first reordering: add `order-first md:order-last` to the element that should appear first on mobile, and `order-last md:order-first` to the element that should appear second
  - This pattern only works inside a flex or grid container — the existing `grid grid-cols-1 md:grid-cols-2` satisfies this requirement
---

## 2026-06-13 - US-037
- Removed AnimatedSection import and wrapper from ServiceSectionBlock in app/services/page.tsx
- AnimatedSection wrapped the grid div inside each ServiceSectionBlock; replaced with the grid div directly
- StickyServiceNav and its IntersectionObserver logic were untouched
- Files changed: app/services/page.tsx
- Typecheck passes; manual browser verification recommended
- **Learnings for future iterations:**
  - When removing AnimatedSection that wraps an inner div, merge the wrapper removal carefully — the replacement is just removing the outer element, not replacing it with another div wrapper
  - Watch for JSX nesting errors when removing a wrapper: removing `<AnimatedSection>` + inner `<div>` and replacing with just `<div>` means one fewer closing tag is needed
---

## Codebase Patterns
- AnimatedSection wraps content at section level (heading + grid/list) using opacity:0 initial state; removing it is a direct import removal + replace wrapper with plain div
- Individual motion.div elements using whileInView (FAQItem, CustomerJourneySection, DashboardVisual) are NOT AnimatedSection wrappers — leave them untouched
- Typecheck is `npx tsc --noEmit`; no test suite to run

# Ralph Progress Log
Started: Sat Jun 13 17:15:05 PST 2026
---

## 2026-06-13 - US-036
- Removed AnimatedSection wrappers from DashboardSection, ServicesSection, HowItWorksSection, WhyUsSection, FAQSection, CTABand
- Replaced AnimatedSection wrappers with plain divs in all 6 components
- Files changed: components/DashboardSection.tsx, components/ServicesSection.tsx, components/HowItWorksSection.tsx, components/WhyUsSection.tsx, components/FAQSection.tsx, components/CTABand.tsx
- curl http://localhost:3000 | grep 'opacity: 0' → 0 matches
- Typecheck passes
- Browser testing unavailable (Playwright/Chrome not installed); manual verification recommended
- **Learnings for future iterations:**
  - All 6 section changes were already partially staged; some components had changes in both staged/unstaged state — always check `git diff HEAD` and `git diff --staged` before working
  - The AnimatedSection import line is always at the top of the file; removing it is the first edit, then replace each `<AnimatedSection>` + `</AnimatedSection>` pair with `<div>` + `</div>` (keeping all classNames on child divs)
  - FAQItem's `motion.div initial={{ opacity: 0, y: 20 }} whileInView={...}` is individual-element animation, NOT a section-level AnimatedSection wrapper — do not touch
  - CustomerJourneySection motion.divs are also decorative/individual — do not touch
---
