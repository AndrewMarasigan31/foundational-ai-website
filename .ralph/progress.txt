# Ralph Progress Log
Started: Sun Jun 14 11:18:36 PST 2026
---

## Codebase Patterns
- Hero and all section components are in /components/*.tsx (no src/ directory)
- Tailwind arbitrary values use bracket notation: tracking-[-0.05em], text-[#C9A227] etc.
- useReducedMotion from framer-motion is used throughout — always preserve reduced-motion guards
- AnimatedSection wrapper is in components/AnimatedSection.tsx
- Typecheck: run `npx tsc --noEmit` from project root

## 2026-06-14 - US-071
- Changed Hero H1 from font-extrabold to font-light
- Updated letter-spacing from tracking-tight to tracking-[-0.05em]
- Updated font sizes from text-4xl sm:text-5xl lg:text-6xl to text-5xl sm:text-6xl lg:text-7xl
- Updated line height from leading-tight to leading-none
- ScrambleWord, line-lift animation, and useReducedMotion guard all preserved
- Files changed: components/HeroSection.tsx (line 148)
- **Learnings for future iterations:**
  - H1 is on a single line (line 148), easy to find
  - All animation logic is inline in HeroSection.tsx, not in a separate file
---

## 2026-06-14 - US-072
- Rewrote Hero H1 from "Your Competitors / Rank Higher. / That's Fixable." to "Rank First. / Win Local."
- "Local." is now the ScrambleWord with gold em accent (font-serif not-italic text-[#C9A227])
- Simplified from 3-line array approach to explicit two-span blocks (cleaner since lines have different structures)
- Subtext paragraph below headline is unchanged
- Files changed: components/HeroSection.tsx
- **Learnings for future iterations:**
  - H1 had 3 lines: 2 in an array loop + 1 special line with ScrambleWord — refactored to 2 explicit spans
  - ScrambleWord component lives at top of HeroSection.tsx and is straightforward to reuse
---

## 2026-06-14 - US-073
- Added eyebrow label '<p>LOCAL SEO · AI SEARCH · RESULTS</p>' immediately above the H1 in HeroSection.tsx
- Used motion.p (with reduced-motion fallback as plain <p>) — fade-in from opacity 0 + y 6 with delay 0
- Styling: text-xs uppercase tracking-[0.2em] text-[#99907b]
- Files changed: components/HeroSection.tsx
- **Learnings for future iterations:**
  - Eyebrow elements follow the same reduced-motion dual-branch pattern used for all animated elements in HeroSection
  - motion.p is the correct element for animated paragraph eyebrows (not motion.div wrapping a p)
---

## 2026-06-14 - US-074
- Added ghost watermark 'RANK' div as first child of the relative z-10 grid container in HeroSection
- Styled: absolute select-none pointer-events-none text-[20vw] font-black text-white/[0.03] leading-none top-0 -left-4 aria-hidden="true"
- No animation needed (criteria explicitly states plain static div is correct)
- The section already has overflow-hidden so no layout shift or scrollbar issues
- Files changed: components/HeroSection.tsx
- **Learnings for future iterations:**
  - The inner `div.relative.z-10` grid container is the correct anchor for absolutely-positioned decorative layers
  - Section already has overflow-hidden so overflow from large text-[20vw] elements is safely clipped
---

## 2026-06-14 - US-076
- Added eyebrow labels to all 5 major homepage sections
- CustomerJourneySection: '01 — HOW IT WORKS' (both prefersReducedMotion and animated branches)
- ServicesSection: '02 — SERVICES' (plain div header, no AnimatedSection)
- WhyUsSection: '03 — WHY US' (inside AnimatedSection)
- HowItWorksSection: '04 — PROCESS' (inside AnimatedSection)
- FAQSection: '05 — FAQ' (plain div header)
- All eyebrows: text-xs uppercase tracking-[0.2em] text-[#99907b] mb-3
- Files changed: components/CustomerJourneySection.tsx, ServicesSection.tsx, WhyUsSection.tsx, HowItWorksSection.tsx, FAQSection.tsx
- **Learnings for future iterations:**
  - ServicesSection and FAQSection use plain divs (no AnimatedSection) — eyebrow is a plain <p> inside the existing div
  - CustomerJourneySection has both branches (prefersReducedMotion div + motion.div) — always update both
  - Numbering follows PRD spec, not strict page order (03=WhyUs, 04=HowItWorks despite HowItWorks appearing before WhyUs on page)
---

## 2026-06-14 - US-077
- Changed Services H2 from 'Get Found. Convert. Recover What You Left on the Table.' to 'Get Found. Get Paid.'
- 'Get Paid.' wrapped in gold em accent (font-serif not-italic text-[#C9A227]) per pattern
- Subtext paragraph below heading is unchanged
- Files changed: components/ServicesSection.tsx (line 313)
- **Learnings for future iterations:**
  - The Services H2 uses an inline em for the gold accent word — follow same pattern for other copy rewrites
---

## 2026-06-14 - US-082
- Changed grid from grid-cols-4 to grid-cols-2 (2x2 layout)
- Removed AnimatedProcessLine SVG component and its useRef/useEffect/useInView/useAnimation imports
- Replaced SVG connecting line with border-l-2 border-[#C9A227]/30 per-card left accent
- Step cards now left-aligned (removed items-center text-center)
- Step numbers enlarged to text-8xl md:text-9xl (from text-6xl md:text-8xl) and made non-absolute (normal flow)
- Staggered animations preserved via stepDelays array
- Files changed: components/HowItWorksSection.tsx (full rewrite)
- **Learnings for future iterations:**
  - HowItWorksSection had a custom AnimatedProcessLine component using framer-motion hooks — safe to remove if replacing the SVG line
---

## 2026-06-14 - US-081
- Restructured WhyUs into asymmetric bento: card 1 = md:col-span-2 (wide), cards 2+3 stacked in remaining col
- Wide card (card 1): flex-row on desktop with large number (6rem) on left, title+description on right
- Cards 2+3: stacked in a flex-col gap-6 wrapper, keep existing absolute watermark number pattern
- Mobile: all cards collapse to full-width stacked (grid-cols-1)
- Hover effects and staggered entrance animations preserved (delay by index)
- Replaced .map() with explicit wide card + slice(1).map() for the right column
- Files changed: components/WhyUsSection.tsx (full rewrite)
- **Learnings for future iterations:**
  - When cards have different layouts, split out of a single .map() — render card 1 explicitly, then slice(1).map() for rest
  - flex-1 on small cards inside flex-col wrapper ensures they fill equal height
---

## 2026-06-14 - US-080
- Added border-t border-white/10 to WhyUsSection, HowItWorksSection, FAQSection
- ServicesSection skipped (already has distinct dark bg-[#0e2131] that contrasts clearly)
- All three were single-line className changes on the outermost <section> tag
- Files changed: components/WhyUsSection.tsx (line 31), HowItWorksSection.tsx (line 75), FAQSection.tsx (line 117)
- **Learnings for future iterations:**
  - Section border additions are trivial — each section has a single <section> tag as outer wrapper
---

## 2026-06-14 - US-079
- Changed CTABand H2 from 'Start with a free audit call. No pitch, no pressure.' to 'Rank. Convert. Grow.'
- 'Grow.' wrapped in gold em accent (font-serif not-italic text-[#C9A227])
- H2 typography updated: font-extrabold → font-light, tracking-tight → tracking-[-0.04em], leading-tight → leading-none
- Both prefersReducedMotion (static h2) and animated (motion.h2) branches updated
- Subtext paragraph, buttons, and gold radial glow animation are all unchanged
- Files changed: components/CTABand.tsx (lines 37-40, 63-72)
- **Learnings for future iterations:**
  - CTABand has same dual-branch pattern (prefersReducedMotion ? static : animated) — always update both
---

## 2026-06-14 - US-078
- Changed WhyUs card 2 title from 'Built for How Search Actually Works Now' to 'Built for Modern Search'
- Other two card titles (AI-Assisted, Human-Reviewed / No Bloated Layers) and all descriptions unchanged
- Files changed: components/WhyUsSection.tsx (line 15)
- **Learnings for future iterations:**
  - Card data lives in the `reasons` array at the top of WhyUsSection.tsx — quick single-line change
---

## 2026-06-14 - US-075
- Changed H2 typography in ServicesSection, WhyUsSection, HowItWorksSection, FAQSection, CustomerJourneySection
- All top-level section H2s: font-extrabold → font-light, tracking-tight → tracking-[-0.04em]
- Card H3 titles and step number spans (font-extrabold) are unchanged as required
- CustomerJourneySection has two H2 instances (reduced-motion branch and animated branch) — both updated via replace_all
- Files changed: components/ServicesSection.tsx, WhyUsSection.tsx, HowItWorksSection.tsx, FAQSection.tsx, CustomerJourneySection.tsx
- **Learnings for future iterations:**
  - CustomerJourneySection duplicates H2 markup inside a prefersReducedMotion conditional — always check for duplicate elements there
  - The consistent pattern `text-3xl sm:text-4xl lg:text-5xl font-display font-extrabold ... tracking-tight` appears in 5 sections; replace_all is safe within a single file but must be verified
---
